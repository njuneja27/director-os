{
  "name": "@director-os/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/main.js",
  "scripts": {
    "build": "npm --prefix ../../packages/shared run build && npm --prefix ../../packages/core run build && tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch --preserveWatchOutput",
    "dev": "electron .",
    "start": "electron .",
    "typecheck": "npm --prefix ../../packages/shared run build && npm --prefix ../../packages/core run build && tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@director-os/core": "file:../../packages/core",
    "@director-os/shared": "file:../../packages/shared",
    "electron": "^33.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "typescript": "^5.8.2"
  }
}
