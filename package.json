{
  "name": "director-os",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first CLI and light web UI for running an AI-assisted software operating loop.",
  "type": "module",
  "workspaces": [
    "apps/cli",
    "apps/web",
    "apps/desktop",
    "packages/shared",
    "packages/core"
  ],
  "scripts": {
    "build": "npm --prefix packages/shared run build && npm --prefix packages/core run build && npm --prefix apps/web run build && npm --prefix apps/desktop run build && npm --prefix apps/cli run build",
    "desktop:build": "npm --prefix packages/shared run build && npm --prefix packages/core run build && npm --prefix apps/web run build && npm --prefix apps/desktop run build",
    "desktop:start": "npm run desktop:build && npm --prefix apps/desktop run start",
    "desktop:dev": "concurrently -k -n renderer,desktop-ts,electron -c auto \"npm --prefix apps/web run dev -- --host 127.0.0.1\" \"npm --prefix apps/desktop run build:watch\" \"wait-on tcp:4310 file:apps/desktop/dist/main.js && VITE_DEV_SERVER_URL=http://127.0.0.1:4310 npm --prefix apps/desktop run dev\"",
    "typecheck": "npm --prefix packages/shared run build && npm --prefix packages/core run build && npm --prefix apps/web run typecheck && npm --prefix apps/desktop run typecheck && npm --prefix apps/cli run typecheck",
    "dev": "npm --prefix apps/web run dev",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.13.14",
    "@types/react": "^19.0.10",
    "@types/react-dom": "^19.0.4",
    "@vitejs/plugin-react": "^4.3.4",
    "concurrently": "^9.1.2",
    "typescript": "^5.8.2",
    "vite": "^6.2.1",
    "vitest": "^3.0.9",
    "wait-on": "^8.0.3"
  }
}
